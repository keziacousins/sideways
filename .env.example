# ── Public-facing URLs (what the browser sees) ──────────────────────
PUBLIC_URL=http://localhost:4000
PUBLIC_API_URL=http://localhost:4100

# ── Internal service URLs (server-to-server) ────────────────────────
DATABASE_URL=postgres://sideways:sideways@localhost:5432/sideways
SEAWEEDFS_FILER_URL=http://localhost:8888
SEAWEEDFS_MASTER_URL=http://localhost:9333
KRATOS_PUBLIC_URL=http://localhost:4433
KRATOS_ADMIN_URL=http://localhost:4434
HYDRA_PUBLIC_URL=http://localhost:4444
HYDRA_ADMIN_URL=http://localhost:4445

# ── Hydra issuer (must match JWT "iss" claim) ───────────────────────
# In production behind nginx: https://docs.example.com/auth/oauth2
HYDRA_ISSUER_URL=http://localhost:4444

# ── Secrets (CHANGE ALL OF THESE for production) ────────────────────
POSTGRES_PASSWORD=sideways
HYDRA_SYSTEM_SECRET=generate-a-random-secret-here
KRATOS_COOKIE_SECRET=generate-a-random-secret-here
KRATOS_CIPHER_SECRET=must-be-exactly-32-characters!!

# ── SMTP (for password recovery emails) ─────────────────────────────
# Dev: mailhog at localhost:1025 (UI at :8025)
# Prod: smtp://user:pass@smtp.example.com:587
SMTP_URL=smtp://localhost:1025/?disable_starttls=true
SMTP_FROM=noreply@sideways.dev

# ── Docker Compose (infra/) ─────────────────────────────────────────
# API_INTERNAL_URL is how containers reach the API server
# Dev: host.docker.internal (API runs on host machine)
# Prod: http://api:4100 (API runs in same Docker network)
API_INTERNAL_URL=http://host.docker.internal:4100
